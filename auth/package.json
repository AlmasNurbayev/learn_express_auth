{
  "name": "auth",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint . --ext .ts",
    "start": "tsx index.ts",
    "watch": "tsx watch --clear-screen=false index.ts",
    "drizzle:generate": "drizzle-kit generate:pg",
    "drizzle:push": "drizzle-kit push:pg",
    "drizzle:deploy": "npm run drizzle:generate && npm run drizzle:push"
  },
  "scriptsComments": {
    "drizzle": "run all db commands only INTO DOCKER SHELL EXEC, not from vs:code"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/pg": "^8.10.9",
    "@typescript-eslint/eslint-plugin": "^6.18.1",
    "drizzle-kit": "^0.20.10",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-prettier": "^5.1.2",
    "tsx": "^4.7.0"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "drizzle-orm": "^0.29.3",
    "express": "^4.18.2",
    "pg": "^8.11.3"
  }
}
